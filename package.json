{
  "name": "tabol",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt && python3 manage.py collectstatic --noinput"
  }
}
